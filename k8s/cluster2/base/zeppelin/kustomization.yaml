apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: arbitrary

resources:
  - spark-setup.yaml
  - zeppelin-auth.yaml
  - persistent-volume-claim.yaml
  - zeppelin-server.yaml
  - zeppelin-ingress.yaml
